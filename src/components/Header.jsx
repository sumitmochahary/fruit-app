import SearchBar from "./SearchBar";

function Header() {

    return (
      <header className="App-header">
        <h1>Fruit Basket</h1>
        <SearchBar />
      </header>
    );
}

export default Header